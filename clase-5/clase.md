#Clase 5

> ECMA6 

1. **ECMA6**
    Opciones para transpilar ES6 --> ES5
    - Babel
    - Webpack, Gulp, Grunt (Task runenrs)
    - Plugin en tu editor
    - App con interfaz gráfica (GUI)  [babel GUI](https://babeljs.io/repl)    


 >user strict es un string que se pone al principio para programar un poquito mejor, nos avisa de malas prácticas. 



const = es una varialble que no le puedo volver a dar valor, también podemos crearlo como un objeto

let = es una variable que si que podemos modificar su valor, pero su scope es local

Arrow Function = es una función anónima e inline (una linea), tiene una primera parte entre parentesis, la anotación flecha y luego las llaves en las que irá nuestro código.
Aceptan parámetros, cuando es un solo parámetro el parentesis es opcional.
Cuando va con llaves es porque tiene sentencias y aquí si que nos hace falta rl return.
() => {}

Arguments es una array para los parámetros que hay en nuestra función.

Parámetros adicionales los ... nos permite añadir los parametros que queramos 

PLantillas de string = (backticks) ´´
var cadena = ´hola 
que
haces
´




> Ejercicios 
Desde la página http://es6katas.org/ hacer las siguientes Katas
- Block Scope
- Arrow Functions
- Default parameters
- Template string (easy)




